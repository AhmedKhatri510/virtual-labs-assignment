import arr from"../constants/tableData.js";export default function filterOnDate(e="",t=""){let a=[];return 0===e.length&&0===t.length?arr:(0!==e.length&&0!==t.length&&(a=arr.filter((a=>new Date(e)<=new Date(a.date)&&new Date(t)>=new Date(a.dueDate)))),a)}